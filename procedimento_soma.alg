algoritmo "procedimento_soma"
// Função :
// Autor :
// Data : 25/11/2020
// Seção de Declarações
var
d,f,x : inteiro
procedimento soma(a, b : inteiro)
inicio
x <- a + b
escreval("recebi o valor ", a)
escreval("recebi o valor ", b)
escreval("Soma entre ", a,  " +", b, " é ", x)
fimprocedimento
inicio
// Seção de Comandos
d <- 5
f <- 10
soma(d, f)
fimalgoritmo
